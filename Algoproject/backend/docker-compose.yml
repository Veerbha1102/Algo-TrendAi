version: '3.8'

services:
  algopilot-engine:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: always

  algopilot-bot:
    build: 
      context: .
      dockerfile: Dockerfile.bot
    env_file:
      - .env
